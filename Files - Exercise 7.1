#Exercise 7.1
#Write a program to read through a ?le and print the contents of the ?le (line by line) all in upper case.'''
my_file = open("D:\mbox-short.txt", "r")
print my_file
count = 0
for line in my_file:
  line = line.strip() #Strips Whitespace on the both sides. To be specific, use rstrip() or lstrip() for either right or left respectively.
  print line.upper() #Covert contents of my_file into Uppercase only.
